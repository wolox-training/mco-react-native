import { StyleSheet } from 'react-native';

const ICON_SIZE = 25;
export default StyleSheet.create({
  image: {
    height: ICON_SIZE,
    width: ICON_SIZE,
    marginBottom: 15,
    marginRight: 10
  }
});
